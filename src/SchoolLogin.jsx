import{useState}from'react';import{useNavigate}from'react-router-dom';import{loginWithSchoolId}from'./services/loginApi';const IMG='/dps-logo.jpeg',labels={STUDENT:'Student',TEACHER:'Teacher',PARENT:'Parent',ACCOUNTANT:'Accountant',ADMIN_STAFF:'Administrative Staff',ADMINISTRATOR:'Administrator',SUPER_ADMIN:'Super Administrator'},ids={STUDENT:'DPS202601',PARENT:'DPS202601',TEACHER:'DPST001',ACCOUNTANT:'DPSA001',ADMIN_STAFF:'DPSS001',ADMINISTRATOR:'DPSADM001',SUPER_ADMIN:'DPSSA001'},dobs={STUDENT:'2010-04-15',PARENT:'2010-04-15',TEACHER:'1985-01-15',ACCOUNTANT:'1988-06-20',ADMIN_STAFF:'1990-02-10',ADMINISTRATOR:'1982-09-05',SUPER_ADMIN:'1980-01-01'};
export default function SchoolLogin(){const[role,setRole]=useState('STUDENT'),[loginId,setLoginId]=useState(ids.STUDENT),[dob,setDob]=useState(dobs.STUDENT),[error,setError]=useState(''),[busy,setBusy]=useState(false),nav=useNavigate();function change(v){setRole(v);setLoginId(ids[v]);setDob(dobs[v])}async function submit(e){e.preventDefault();setBusy(true);setError('');try{const d=await loginWithSchoolId(role,loginId,dob);sessionStorage.setItem('dps-token',d.token);sessionStorage.setItem('dps-user',JSON.stringify(d.user));nav('/erp')}catch(e){setError(e.message)}finally{setBusy(false)}}const student=['STUDENT','PARENT'].includes(role);return <main className="login"><div className="loginbrand"><a href="/">← Back to website</a><img src={IMG}/><span className="kicker lighttxt">Secure school access</span><h1>One identity, <em>your school world.</em></h1><p>Use the school-issued ID and registered date of birth.</p></div><form onSubmit={submit}><div className="formhead"><img src={IMG}/><div><h2>Welcome back</h2><p>Sign in to the DPS ERP</p></div></div><label>Your role<select value={role} onChange={e=>change(e.target.value)}>{Object.entries(labels).map(([v,n])=><option key={v} value={v}>{n}</option>)}</select></label><label>{student?'Admission number':'School staff ID'}<input value={loginId} onChange={e=>setLoginId(e.target.value.toUpperCase())} required/></label><label>Date of birth<input value={dob} onChange={e=>setDob(e.target.value)} required type="date"/></label>{error&&<p className="formerror">{error}</p>}<button disabled={busy} className="btn full">{busy?'Verifying…':'Sign in securely'}</button><p className="demo">Demo credentials are prefilled for the selected role.</p></form></main>}
