const API=(import.meta.env.VITE_API_BASE_URL||'/api').replace(/\/$/,'');
function normalizeDob(value){const raw=String(value||'').trim();if(/^\d{4}-\d{2}-\d{2}$/.test(raw))return raw;const match=raw.match(/^(\d{1,2})[-/.](\d{1,2})[-/.](\d{4})$/);if(!match)return raw;const[,day,month,year]=match;return `${year}-${month.padStart(2,'0')}-${day.padStart(2,'0')}`}
export async function loginWithSchoolId(role,loginId,dob){let res;try{res=await fetch(`${API}/auth/login`,{method:'POST',headers:{'Content-Type':'application/json'},credentials:'same-origin',body:JSON.stringify({role,loginId:loginId.trim(),dateOfBirth:normalizeDob(dob)})})}catch{throw new Error('Cannot connect to the school server')}if(!(res.headers.get('content-type')||'').includes('application/json'))throw new Error('Backend API is not configured');const data=await res.json();if(!res.ok)throw new Error(data.error||'Invalid admission number/school ID or date of birth');return data}
