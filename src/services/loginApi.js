const API=(import.meta.env.VITE_API_BASE_URL||'/api').replace(/\/$/,'');export async function loginWithSchoolId(role,loginId,dob){let res;try{res=await fetch(`${API}/auth/login`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({role:`${role}:${loginId}`,password:dob})})}catch{throw new Error('Cannot connect to the school server')}if(!(res.headers.get('content-type')||'').includes('application/json'))throw new Error('Backend API is not configured');const data=await res.json();if(!res.ok)throw new Error('Invalid admission number/school ID or date of birth');return data}
